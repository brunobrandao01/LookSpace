name: Auto Sync Validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate-structure:
    name: Validate LookSpace structure
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate required directories and documentation
        shell: pwsh
        run: |
          $requiredDirs = @(
            'client',
            'server',
            'universe',
            'ai',
            'website',
            'assets',
            'scripts',
            'shaders',
            'systems',
            'multiplayer',
            'rendering',
            'cinematic',
            'planets',
            'spaceships',
            'cameras',
            'lighting',
            'environments',
            'unreal',
            'visual-docs',
            'docs'
          )
          foreach ($dir in $requiredDirs) {
            if (-not (Test-Path $dir)) {
              throw "Missing required directory: $dir"
            }
          }

          $requiredFiles = @(
            'README.md',
            '.gitignore',
            'CONTRIBUTING.md',
            'LICENSE',
            'docs/README.md',
            'docs/ARCHITECTURE.md',
            'docs/ROADMAP.md',
            'docs/PIPELINE.md',
            'docs/PROJECT_OVERVIEW.md',
            'docs/GIT_AUTOSYNC.md',
            'docs/UNREAL_SETUP.md',
            'docs/UNREAL_INTEGRATION.md',
            'docs/AAA_VISUAL_ROADMAP.md',
            'docs/RENDERING_WORKFLOW.md',
            'docs/DEEP_SPACE_RENDERING.md',
            'docs/EARTH_RENDERING.md',
            'docs/CINEMATIC_LIGHTING.md',
            'docs/CAMERA_SYSTEMS.md',
            'docs/SPACESHIP_PROTOTYPE.md',
            'docs/SHADER_ARCHITECTURE.md',
            'docs/UNIVERSE_ENGINE.md',
            'docs/PROCEDURAL_GENERATION.md',
            'docs/RENDERING_ENGINE.md',
            'docs/SPACESHIP_SYSTEMS.md',
            'docs/MULTIPLAYER_ARCHITECTURE.md',
            'docs/AI_SYSTEMS.md',
            'docs/CINEMATIC_RENDERING.md'
          )
          foreach ($file in $requiredFiles) {
            if (-not (Test-Path $file)) {
              throw "Missing required file: $file"
            }
          }
